# La libreria Matrix.h permette di creare matrici dimensione NxM realizza le principali operazioni tra matrici come somma, sottrazione moltiplicazione per scalare e moltiplicazioni tra matrici.
# Implementa il calcolo del determinante, della matrice trasposta e inversa (dove possibile).
# Implementa tre metodi per la risoluzione di sistemi lineari Ax=b: solutore con operatore di tipo backslash di matlab, metodo di eliminazione di Gauss e backward resolution, fattorizzazione LU con permutazioni P.
# Implementa delle funzioni per la creazione di matrici di rotazione e traslazione omogenee in 2D/3D, e di matrici di rotazione composte secondo la convenzione ZYZ e ZYX.
